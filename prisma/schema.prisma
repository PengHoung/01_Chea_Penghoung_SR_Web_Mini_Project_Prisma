

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model SrStudent {
  id        Int  @id @unique @default(autoincrement())
  name      String
  email     String   @unique
  gender    String
  @@map("sr_students")
}